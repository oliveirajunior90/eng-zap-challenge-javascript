import { withStyles } from '@material-ui/core';

const styles = () => ({
  wrapper: {
    background: '#eaeaea',
    padding: '40px 0',
    minHeight: '70px',
  },

  image: {
    width: '70px',
  },
});

export default withStyles(styles);
