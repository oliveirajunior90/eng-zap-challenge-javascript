import { withStyles } from '@material-ui/core/';

const styles = {
  wrapper: {
    marginTop: '25px',
    marginBottom: '25px',
  },

  pagination: {
    margin: '20px 0 20px',
    display: 'flex',
    justifyContent: 'center',
  },
};

export default withStyles(styles);
